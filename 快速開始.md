# VitalMesh 快速開始

本指南用於第一次在 Android 手機的 OperitAI 上使用 VitalMesh Health Bridge，並讓 Home Assistant iOS 將健康資料同步到 Android 本機後端。

## 1. 下載 Home Assistant iOS

在 iPhone 上安裝官方 Home Assistant iOS App：

- App Store：<https://apps.apple.com/app/home-assistant/id1099568401>
- 官方文件：<https://companion.home-assistant.io/docs/core>

Android 與 iPhone 必須連接同一個區域網路，例如同一個 Wi-Fi。不要使用訪客 Wi-Fi，部分路由器會隔離不同裝置。

## 2. 啟動 Android 後端

在 Android 的 OperitAI 中：

1. 開啟側邊欄。
2. 點選「VitalMesh 健康」。
3. 點選「啟動伺服器」。
4. 等待頁面顯示 VitalMesh 儀表台。

如果側邊欄沒有顯示 VitalMesh：

1. 進入 OperitAI 的工具包／插件管理。
2. 找到 `VitalMesh Health Bridge` 或 `VitalMesh 健康橋接`。
3. 手動進入插件頁面。
4. 點選「啟動伺服器」。
5. 啟動成功後再回到側邊欄開啟 WebView。

後端會由 ToolPkg 內置的 Node.js 自動啟動，不需要另外安裝 Node.js、npm 或 npm 套件。

## 3. 找到 Android 網路 IP

### 方法 A：使用 Android 系統設定

在 Android 手機開啟：

`設定` → `WLAN／Wi-Fi` → 點選目前連線的網路 → 查看「IP 位址」。

例如：

```text
192.168.31.233
```

### 方法 B：使用 OperitAI 內建終端

在 Ubuntu 終端執行：

```bash
ip -4 addr
```

找出目前區域網路介面的 `inet` 位址，排除 `127.0.0.1`。例如：

```text
inet 192.168.31.233/24
```

其中 Android 的網路 IP 是：

```text
192.168.31.233
```

不要使用 `127.0.0.1` 填給 iPhone，因為那代表 iPhone 自己，而不是 Android 手機。

## 4. iOS 端填寫地址

在 Home Assistant iOS 的伺服器地址／連線地址欄位填寫：

```text
http://Android網路IP:8123
```

例如 Android IP 是 `192.168.31.233`，就填：

```text
http://192.168.31.233:8123
```

端口固定為：

```text
8123
```

不要填入：

```text
http://127.0.0.1:8123
```

也不要填 VPS 地址。這個手機版 ToolPkg 的後端運行在 Android 本機，iPhone 必須透過 Android 的區域網路 IP 連線。

## 5. 連線檢查

在 Android 端先確認服務是否啟動。OperitAI 側邊欄頁面應該能開啟：

```text
http://127.0.0.1:8123/
```

從同一個區域網路的其他裝置測試時，使用：

```text
http://Android網路IP:8123/
```

例如：

```text
http://192.168.31.233:8123/
```

如果無法連線，依序檢查：

- Android 與 iPhone 是否連接同一個 Wi-Fi。
- Android IP 是否因切換 Wi-Fi 而改變。
- OperitAI 裡是否已點選「啟動伺服器」。
- 地址是否使用 `http://` 而不是 `https://`。
- 端口是否為 `8123`。
- 路由器是否啟用了 AP 隔離／用戶端隔離。

## 6. 健康資料工具

ToolPkg 內提供四個健康資料工具：

- `list_devices`
- `list_sensors`
- `get_sensor_latest`
- `get_sensor_history`

工具會優先使用本機 REST API；舊版後端或 VPS 版本仍可使用 MCP `/mcp` 協議作為相容回退。

## 7. 注意事項

- 本機 ToolPkg 預設不設定 Token。
- VPS 部署版可以獨立設定 Token，與本機 ToolPkg 的設定分開。
- Android 的網路 IP 可能在重新連線 Wi-Fi 後改變，IP 改變時需要同步更新 iOS 端地址。
- 後端只在點選「啟動伺服器」後啟動，不會因單純安裝插件而自動佔用 `8123`。
